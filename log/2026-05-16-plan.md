# 开发计划 2026-05-16

## 优先级高

### 1. 数据库索引优化
- [ ] 在云开发控制台为 `diaries` 集合创建组合索引：`date` 降序 + `date` 升序
- [ ] 为 `todos` 集合创建组合索引：`date` 升序 + `priority` 降序
- [ ] 为 `anniversaries` 集合创建索引：`date` 升序
- [ ] 为 `tags` 集合创建索引：`createdAt` 降序

### 2. `Error: timeout` 排查
- [ ] 确认 timeout 来源（微信开发者工具自身 / 云开发调用）
- [ ] 如果是云开发超时，检查数据库请求是否需要优化
- [ ] 考虑添加请求超时重试机制

### 3. 真机测试
- [ ] 在微信开发者工具中使用真机调试模式
- [ ] 测试核心流程：写日记 → 查看 → 编辑 → 删除
- [ ] 测试待办：添加 → 勾选完成 → 切换日期
- [ ] 测试日历：月份切换 → 日期选中 → 查看详情
- [ ] 测试标签：创建 → 删除 → 按标签筛选
- [ ] 测试纪念日：添加 → 删除 → 倒计时计算
- [ ] 测试退出登录 → 重新登录

## 优先级中

### 4. 安全规则加固
- [ ] 检查云数据库安全规则，确保用户只能访问自己的数据
- [ ] 为 `diaries`、`todos`、`tags`、`anniversaries` 集合添加 `openid` 校验

### 5. 微信小程序审核准备
- [ ] 检查隐私政策
- [ ] 检查用户协议
- [ ] 确保登录流程符合微信要求

## 优先级低

### 6. API 层解码验证
- [ ] 确认 API 层 `decodeHtml()` 在实际数据上正常工作
- [ ] 移除调试日志 `console.log('[API] decode:', ...)`

### 7. Code Cleanup
- [ ] 检查是否有未使用的组件（如 `DiaryCard.vue`）
- [ ] 统一代码风格
