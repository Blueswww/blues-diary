# 开发日志 2026-05-17

## 今日完成

### 待办提醒功能（方向一）

**数据层**
- `src/api/todo.ts` — TodoRecord 新增 `reminder`（`{ enabled, time }`）、`reminded` 字段；新增 `updateTodoReminder()` API
- `cloudfunctions/sendReminder/` — 新建云函数，通过 `cloud.openapi.subscribeMessage.send()` 发送订阅消息，关键词适配用户模板（thing2/date4/thing11/time30）
- `cloudbaserc.json` — 新增 `sendReminder` 云函数条目并部署

**状态层**
- `src/store/todo.ts` — 新增 `setReminder()`、`checkReminders()`（每分钟匹配）、`startReminderCheck()`/`stopReminderCheck()`（30 秒轮询）
- 默认模板 ID 硬编码为 `mP9JdbNYlo-2QvknNlB3DBNLtbKv0kkFJDbUvrvHbus`

**UI 层**
- `src/pages/todo/todo.vue` — 每条待办右侧添加 🔔 圆形按钮（未设置灰框、已设置橙框、点击时蓝框），点击弹出 `picker` 时间选择器 + 确认/取消按钮
- `src/pages/mine/mine.vue` — 移除待办提醒设置入口（模板 ID 硬编码，无需用户配置）

**修复**
- 用户点击"确认"时立即调用 `wx.requestSubscribeMessage()`（必须在用户手势中调用），授权成功后才保存提醒到数据库；定时器触发时仅调用云函数发送，不再请求授权

## 未完成

### 方向二：微信小程序正式上线
- 待方向一测试稳定后，评估整体功能完备性
- 注册微信小程序账号、完善基本信息
- 提交代码审核、正式发布

### 方向三：网页版同步
- 构建 H5 版本（`dev:h5` / `build:h5`）
- 将云数据库替换为可跨端访问的后端方案
- 实现 PC 端浏览器与小程序端数据同步

## 待优化

- **已完成待办不再提醒**：如果在提醒时间到达之前用户已完成该待办，则跳过发送订阅消息。`checkReminders()` 中增加 `!todo.isDone` 过滤条件
